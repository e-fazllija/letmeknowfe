# Default feature flags for shared environments
# Keep V2 detail disabled by default until BE validation completes
VITE_DETAIL_V2=false
# CSRF token header disabled by default; enable only if BE requires it
VITE_CSRF_PROTECTION=false
